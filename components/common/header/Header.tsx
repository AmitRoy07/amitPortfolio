import React from 'react';
import global from 'theme/global/Global.module.scss';
import styles from './Header.module.scss';

const Header = () => {
    return <div className={styles.header_sec}>Header</div>;
};

export default Header;
