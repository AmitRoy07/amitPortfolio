import React from 'react';
import global from 'theme/global/Global.module.scss';
import styles from './Footer.module.scss';

const Footer = () => {
    return <div className={`${styles.footer_sec}`}>Footer</div>;
};

export default Footer;
